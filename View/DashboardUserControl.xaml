<UserControl x:Class="Teste_Técnico.DashboardUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#F5F6FA">
        <StackPanel Margin="20">

            <!-- Título -->
            <TextBlock Text="Dashboard" FontSize="26" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Visão geral do sistema" Margin="0,0,0,20" FontSize="14" Foreground="#7F8C8D"/>

            <!-- Resumos -->
            <UniformGrid Columns="4" Margin="0,0,0,20" HorizontalAlignment="Stretch">

                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Padding="15" Margin="5">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Pessoas Cadastradas" FontWeight="SemiBold" FontSize="14" Foreground="#2C3E50" Margin="0,0,0,10"/>
                        <TextBlock Text="0" FontSize="28" FontWeight="Bold" Foreground="#3498DB"/>
                    </StackPanel>
                </Border>

                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Padding="15" Margin="5">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Produtos Cadastrados" FontWeight="SemiBold" FontSize="14" Foreground="#2C3E50" Margin="0,0,0,10"/>
                        <TextBlock Text="0" FontSize="28" FontWeight="Bold" Foreground="#3498DB"/>
                    </StackPanel>
                </Border>

                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Padding="15" Margin="5">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Total de Pedidos" FontWeight="SemiBold" FontSize="14" Foreground="#2C3E50" Margin="0,0,0,10"/>
                        <TextBlock Text="0" FontSize="28" FontWeight="Bold" Foreground="#3498DB"/>
                    </StackPanel>
                </Border>

                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Padding="15" Margin="5">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Valor Total em Vendas" FontWeight="SemiBold" FontSize="14" Foreground="#2C3E50" Margin="0,0,0,10"/>
                        <TextBlock Text="R$ 0,00" FontSize="28" FontWeight="Bold" Foreground="#3498DB"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Pedidos -->
            <UniformGrid Columns="2" Margin="0,0,0,20">
                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Padding="15" Margin="5">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="⚠ Pedidos Pendentes" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
                        <TextBlock Text="Quantidade: 0 pedidos" Foreground="#7F8C8D"/>
                        <TextBlock Text="Valor Total: R$ 0,00" Foreground="#7F8C8D"/>
                    </StackPanel>
                </Border>

                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Padding="15" Margin="5">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="✔ Pedidos Pagos" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
                        <TextBlock Text="Quantidade: 0 pedidos" Foreground="#7F8C8D"/>
                        <TextBlock Text="Valor Recebido: R$ 0,00" Foreground="#7F8C8D"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Ações Rápidas -->
            <TextBlock Text="Ações Rápidas" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
            <UniformGrid Columns="3">
                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Padding="15" Margin="5">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Cadastrar Pessoa" FontSize="16" FontWeight="Bold" Foreground="#3498DB"/>
                        <TextBlock Text="Adicionar nova pessoa ao sistema" Foreground="#7F8C8D" FontSize="13"/>
                    </StackPanel>
                </Border>

                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Padding="15" Margin="5">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Cadastrar Produto" FontSize="16" FontWeight="Bold" Foreground="#3498DB"/>
                        <TextBlock Text="Adicionar novo produto ao catálogo" Foreground="#7F8C8D" FontSize="13"/>
                    </StackPanel>
                </Border>

                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Padding="15" Margin="5">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Novo Pedido" FontSize="16" FontWeight="Bold" Foreground="#3498DB"/>
                        <TextBlock Text="Criar pedido para um cliente" Foreground="#7F8C8D" FontSize="13"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
